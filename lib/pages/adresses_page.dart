import 'package:flutter/material.dart';

class AdressesPage extends StatelessWidget {
  const AdressesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Adresses Page'),
    );
  }
}